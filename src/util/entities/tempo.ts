export type Tempo = {
    tempo: {
        horario: string;
        materia: string;
        professor: string;
        sala: string;
        isBreak: boolean;
    };
    turma: string;
    id: string
};